# now these are subpackages of the current parent package
# its is important that these also have a __init__.py file to define them as modules
# in that, you can import the files as submodules
import src 
import utils 